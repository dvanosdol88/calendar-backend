# OpenAI API Key (existing)
OPENAI_API_KEY=your_openai_api_key_here

# Google OAuth Credentials (for Gmail and Calendar)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=https://calendar-backend-xwk6.onrender.com/auth/google/callback

# Gmail refresh token (will be set after first auth)
GMAIL_REFRESH_TOKEN=

# Google Calendar Integration
# Choose ONE of the following authentication methods:

# Method 1: Service Account (Recommended for server-side automation)
# Download the service account JSON file from Google Cloud Console
GOOGLE_SERVICE_ACCOUNT_KEY=./path/to/service-account-key.json

# Method 2: OAuth 2.0 User Authentication (for user-specific calendars)
# These are the same as Gmail credentials above, included here for clarity
# GOOGLE_CLIENT_ID=your_google_client_id_here
# GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# GOOGLE_REDIRECT_URI=https://calendar-backend-xwk6.onrender.com/auth/google/callback

# Calendar-specific refresh token (if using OAuth)
GOOGLE_CALENDAR_REFRESH_TOKEN=

# Default timezone for calendar events
DEFAULT_TIMEZONE=America/New_York

# Environment
NODE_ENV=production